let a = 5;
let b = 10;

function sum() {
    console.log("sum", (a + b));
}
sum();//when function is inside the module the code will run even if not assigned nor invoked
//it is there is ES6,vanila JS as well
//Vanila JS is a plain JS without library